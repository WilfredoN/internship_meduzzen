import React from 'react';

const UserProfile = () => {
	return (
		<div className="user-profile flex flex-col items-center justify-center h-screen">
			<h1 className="text-4xl font-bold">User Profile</h1>
			<p className="text-lg mt-4">User Profile</p>
		</div>
	);
};

export default UserProfile;
